@namespace LiveAI.FastBlazor.Components
@inherits Base.FluentComponentBase
@if (!string.IsNullOrEmpty(Icon) && Icon.Trim().StartsWith(("<")))
{
    <svg @attributes="AdditionalAttributes" class="@ClassMapper" style="@StyleMapper" focusable="false" viewBox="@ViewBox" aria-hidden="true" >
        @if (!String.IsNullOrEmpty(Title))
        {
            <title>@Title</title>
        }
        @((MarkupString)@Icon)
    </svg>
}
else
{
    <span @attributes="AdditionalAttributes" class="@ClassMapper" style="@StyleMapper" title="@Title">@((MarkupString)@Icon)</span>
}